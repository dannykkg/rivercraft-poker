# 第一阶段开发方案：传统德州扑克单桌锦标赛

## 1. 阶段目标

第一阶段交付一个可以在现代浏览器中独立运行的传统无限注德州扑克单桌锦标赛游戏。

玩家无需注册或登录，可以直接创建一桌比赛，自定义参赛人数和基础比赛参数，与多个不同难度、不同风格的算法机器人完成一场从开局到决出冠军的锦标赛。

第一阶段同时建立后续 AI 机器人和 AI 教练所依赖的基础平台：

- 确定性扑克规则引擎。
- 统一玩家接口。
- 可复用扑克计算工具。
- 结构化事件日志。
- 存档、恢复和回放能力。
- 玩家视角和隐藏信息隔离。

## 2. 已确认的产品范围

### 2.1 包含范围

- 传统无限注德州扑克（No-Limit Texas Hold'em）。
- 单桌锦标赛（Single Table Tournament）。
- 每桌 2–9 名玩家。
- 每桌固定一名真人玩家，其余座位由算法机器人控制。
- 所有玩家使用相同的初始筹码。
- 小盲和大盲。
- 按完成手数推进的盲注级别。
- 玩家筹码归零后淘汰。
- 最后一名仍有筹码的玩家获得冠军。
- 简单、普通、困难三个机器人难度。
- 多种机器人风格，例如保守、均衡、松散、激进。
- 手牌强度、胜率估算和底池赔率等基础计算。
- 完整牌局事件记录。
- 锦标赛存档、恢复和基础回放。
- 本地设置保存。
- 桌面端和移动端浏览器基本适配。
- GitHub Pages 静态部署。
- 可安装的 PWA 能力。

### 2.2 暂不包含

- 现金桌模式。
- 其他扑克玩法。
- 多人联网。
- 用户账号、登录和云同步。
- 真实货币、支付、买入和提现。
- 重购、增购和赏金赛。
- 多桌锦标赛。
- Ante 和 Big Blind Ante。
- 外部大模型机器人。
- AI 供应商和模型配置界面。
- AI 教练和长期玩家画像。
- GTO 求解器。
- 复杂牌桌动画和重度美术表现。

现金桌应在未来作为独立游戏模式加入。第一阶段的引擎不能把“盲注升级”“淘汰结束”等锦标赛规则硬编码进通用牌局状态机，以免妨碍后续扩展。

## 3. 第一阶段完成标准

第一阶段完成后，用户应当能够：

1. 在浏览器打开游戏，无需注册和配置服务器。
2. 选择 2–9 人牌桌、初始筹码、盲注速度、机器人难度和风格。
3. 完成一场包含淘汰、盲注升级和最终排名的单桌锦标赛。
4. 在自己的行动回合看到全部合法动作和准确的下注范围。
5. 关闭页面后恢复未完成的锦标赛。
6. 查看已经完成的牌局记录并逐步回放公开过程。
7. 查看基础统计和概率信息。
8. 在断网情况下继续使用不依赖网络的游戏功能。

规则引擎应当能够在没有 React、DOM、网络和 IndexedDB 的环境中独立运行全部测试与批量模拟。

## 4. 推荐技术栈

| 领域 | 技术 |
| --- | --- |
| 语言 | TypeScript |
| UI | React |
| 构建 | Vite |
| 应用形态 | Web / PWA |
| 本地数据库 | IndexedDB |
| 单元与集成测试 | Vitest |
| 属性测试 | fast-check |
| 端到端测试 | Playwright |
| 随机数 | Web Crypto；测试中使用可注入的种子随机源 |
| 部署 | GitHub Actions + GitHub Pages |

状态管理优先使用 React 自身的 Reducer 和外部 store 适配层。只有当界面状态复杂度明确需要时，才引入额外状态库。扑克规则状态始终由引擎维护，不能由 UI 状态库替代引擎。

## 5. 总体技术架构

```text
React Web / PWA
├── 首页与新比赛配置
├── 牌桌
├── 牌局记录与回放
├── 基础统计
└── 设置
                ↓
Application
├── TournamentController
├── PlayerController
├── BotScheduler
├── SaveManager
└── ReplayController
                ↓
Domain
├── Tournament Rules
├── Poker Engine
├── Player View Projection
├── GameEvent Log
└── Player Contract
          ↓                 ↓
Poker Tools               Players
├── Hand Evaluator        ├── HumanPlayer
├── Equity Estimator      └── AlgorithmBot
├── Pot Odds
└── Range Utilities
                ↓
Infrastructure
├── IndexedDB Repository
├── Web Crypto RNG
├── PWA Service Worker
└── GitHub Pages Deployment
```

依赖方向只能从上向下。`Domain` 和 `Poker Tools` 不允许依赖 React、IndexedDB 或浏览器界面代码。

## 6. 建议的代码模块

```text
src/
├── app/                 # 应用入口、路由和依赖装配
├── ui/                  # React 页面和组件
├── application/         # 游戏会话、机器人调度、存档和回放编排
├── domain/
│   ├── cards/           # 牌、牌堆和发牌领域类型
│   ├── engine/          # 扑克状态机、动作验证和结算
│   ├── tournament/      # 盲注级别、淘汰、排名和结束条件
│   ├── events/          # GameEvent 定义和 Reducer
│   ├── views/           # 玩家视角和公开视图投影
│   └── players/         # Player 接口和行动意图
├── poker-tools/         # 牌型、胜率、赔率和范围工具
├── bots/                # 算法机器人和策略配置
├── history/             # 牌局摘要、回放和统计派生
├── storage/             # IndexedDB 与版本迁移
└── testing/             # 场景构造器、固定牌堆和测试辅助工具
```

具体目录可以在初始化项目时调整，但引擎、工具、玩家、存储和 UI 之间的边界必须保留。

## 7. 核心领域设计

### 7.1 状态分层

状态分为三个层级：

#### TournamentState

负责整场单桌锦标赛：

- 比赛配置。
- 当前盲注级别。
- 已完成手数。
- 玩家排名和淘汰顺序。
- 当前庄家位置。
- 比赛状态：未开始、进行中、暂停、已结束。

#### TableState

负责牌桌长期状态：

- 座位与玩家。
- 玩家当前筹码。
- 在赛、淘汰和坐出状态。
- 下一手牌的按钮位置。

#### HandState

负责一手牌：

- 当前阶段。
- 当前行动者。
- 玩家底牌和公共牌。
- 本轮及本手投入。
- 当前最高下注。
- 最小加注尺度。
- 主池和边池。
- 已行动、弃牌和全下玩家。
- 摊牌及结算结果。

这种分层使未来现金桌可以复用 `TableState` 和 `HandState`，只替换锦标赛生命周期与筹码补充规则。

### 7.2 状态机阶段

```text
等待开局
  ↓
支付盲注
  ↓
发底牌
  ↓
Pre-flop
  ↓
Flop
  ↓
Turn
  ↓
River
  ↓
Showdown / 其他玩家全部弃牌
  ↓
结算与淘汰
  ↓
移动按钮、更新盲注级别、开始下一手
```

每个阶段只能通过明确的事件转换，不能由 UI 直接跳转。

### 7.3 Command、Event 和 Reducer

外部控制器向引擎提交命令或行动意图：

```text
StartTournament
StartHand
SubmitPlayerAction
PauseTournament
ResumeTournament
```

引擎验证命令后产生领域事件，例如：

```text
TournamentStarted
HandStarted
BlindPosted
HoleCardsDealt
PlayerChecked
PlayerCalled
PlayerRaised
PlayerFolded
CommunityCardsDealt
PotAwarded
PlayerEliminated
BlindLevelAdvanced
TournamentFinished
```

事件经过纯 Reducer 生成新状态：

```text
previousState + event → nextState
```

非法命令返回结构化错误，并且不得生成事件或改变状态。

### 7.4 玩家视角

引擎内部状态与玩家可见状态必须分离。视图投影至少包括：

- `PlayerView`：包含该玩家自己的底牌和公开信息。
- `SpectatorView`：只包含公开信息。
- `ReplayView`：根据回放时间点和公开规则显示信息。
- `InternalView`：仅供规则计算和受控测试使用。

算法机器人也只能通过自己的 `PlayerView` 行动，不能直接读取内部完整状态。

### 7.5 随机性和重放

- 正常游戏使用 Web Crypto 提供随机性。
- 测试和批量模拟允许注入种子随机源。
- 洗牌和发牌结果以可重放事件记录。
- 一旦事件写入日志，回放不再重新随机洗牌。
- 同一初始状态和事件序列必须得到相同结果。

## 8. 传统德州扑克规则范围

第一阶段引擎需要正确处理：

- 2–9 名在赛玩家的按钮和盲注移动。
- Heads-up 时庄家支付小盲及特殊行动顺序。
- Pre-flop 与翻牌后不同的首位行动者。
- Check、Call、Bet、Raise、Fold、All-in。
- 合法最小下注和最小加注。
- 不足额全下。
- 短码全下是否重新开放加注权。
- 多个玩家以不同筹码量全下。
- 主池和多个边池。
- 已弃牌玩家的底池贡献与获奖资格。
- 无人匹配的多余下注返还。
- 所有可行动玩家全下后的自动发牌。
- 其他玩家全部弃牌后的立即结算。
- 摊牌、平局和底池拆分。
- 奇数筹码按明确且可测试的顺序分配。
- 玩家淘汰与最终排名。

所有筹码使用整数，禁止以浮点数表示筹码或下注额。

## 9. 成熟算法的使用方式

### 9.1 牌型计算

牌型计算采用经过验证的成熟实现，通过内部 `HandEvaluator` 接口封装：

```text
evaluate(cards) → HandRank
compare(first, second) → comparison
selectWinners(hands) → winners
```

具体依赖库在开发开始时进行一次小型验证后确定，选择标准包括：

- 支持五至七张牌的标准德州牌型。
- 正确处理 A2345 顺子和所有平局比较。
- 可在浏览器中运行。
- 许可证允许项目使用和分发。
- 有可执行测试或可以与参考结果交叉验证。
- 性能足以支持批量胜率模拟。

引擎只能依赖内部接口，不能让第三方库的数据类型渗透到整个项目。

### 9.2 安全洗牌

采用 Fisher–Yates 洗牌，随机值由可注入的随机源提供：

- 正常运行使用 Web Crypto。
- 测试和复现使用确定性种子随机源。

不得使用 `Math.random()` 作为正式游戏随机源。

### 9.3 胜率估算

第一阶段采用 Monte Carlo 模拟估算当前手牌对随机或基础范围的胜率：

- 复用统一牌型计算器。
- 排除所有已知牌。
- 支持取消和计算预算限制。
- 在 Web Worker 中运行，避免阻塞牌桌界面。
- 返回胜率、平局率、样本数和估算耗时。

基础底池赔率由确定性公式直接计算，不需要模拟。

## 10. 算法机器人方案

所有机器人实现统一接口：

```text
decide(playerView, availableTools, botProfile) → ActionIntent
```

机器人不能访问完整 `GameState`，也不能绕过引擎的动作校验。

### 10.1 难度等级

#### 简单

- 使用基础起手牌分组和当前成牌强度。
- 较少考虑位置和底池赔率。
- 允许受控的非最优行动。
- 不进行长期对手建模。

#### 普通

- 考虑起手牌、位置、筹码深度和底池赔率。
- 使用有限次数的胜率估算。
- 具有基本诈唬和价值下注能力。
- 根据下注尺度做简单范围判断。

#### 困难

- 使用更细致的起手牌范围。
- 综合胜率、底池赔率、位置、有效筹码和行动历史。
- 采用混合策略而不是固定动作。
- 对牌桌玩家形成短期统计，但不使用大模型。

三个难度不承诺 GTO 或理论最优，只要求行为稳定、差异可感知，并且不会读取隐藏信息。

### 10.2 风格参数

难度与风格分离。风格通过参数调整而不是复制多套机器人：

- 入池范围宽度。
- 主动下注和加注倾向。
- 诈唬频率。
- 面对压力时的弃牌倾向。
- 下注尺度偏好。

初始提供保守、均衡、松散和激进风格。允许同一难度下出现不同风格，避免牌桌行为过于一致。

### 10.3 调度与失败回退

- 机器人决策在可取消任务中运行。
- UI 可增加短暂思考延时，但引擎不依赖动画时间。
- 机器人返回非法动作时，先进行一次确定性修正。
- 无法修正时优先过牌；不能过牌时弃牌。
- 所有回退均写入诊断记录，便于测试。

## 11. 锦标赛规则

### 11.1 创建比赛

用户可配置：

- 总玩家人数。
- 初始筹码。
- 初始大小盲。
- 盲注速度：慢速、标准、快速。
- 每个机器人难度和风格。
- 真人玩家座位，或由系统随机分配。

第一版不提供过多高级参数，预设应能一键开始。

### 11.2 盲注升级

盲注按完成手数升级，而不是按现实时间升级，原因包括：

- 暂停和恢复更简单。
- 离线单机体验更稳定。
- 测试和回放完全确定。
- 玩家不会因查看记录或切换页面而损失比赛时间。

不同速度预设只改变每一级持续的手数和盲注表。

### 11.3 淘汰和比赛结束

- 一手牌结算后筹码为零的玩家被淘汰。
- 同一手淘汰多人时，按开局筹码或该手结算规则生成稳定排名；具体规则在引擎规格中固定并测试。
- 真人被淘汰后显示最终名次，并允许选择观看剩余机器人完成比赛。
- 最后一名有筹码的玩家成为冠军。

## 12. 用户界面范围

### 12.1 首页

- 新建锦标赛。
- 继续上次比赛。
- 牌局记录。
- 设置。

### 12.2 新建锦标赛

- 玩家人数。
- 初始筹码和盲注速度预设。
- 真人座位。
- 各机器人难度和风格。
- 一键使用推荐配置。

### 12.3 牌桌

- 座位、名称、筹码和状态。
- 庄家、小盲和大盲标识。
- 真人底牌和公共牌。
- 当前底池及必要的边池信息。
- 当前行动者和最近动作。
- Fold、Check、Call、Bet、Raise、All-in 控件。
- 合法最小值、最大值和快捷下注尺度。
- 当前盲注级别、剩余玩家和下一次升级信息。
- 暂停、保存、退出和基础概率面板。

### 12.4 牌局记录与回放

- 按比赛和手牌浏览。
- 显示按钮、盲注、公共牌、行动和结算。
- 上一步、下一步和跳转到阶段。
- 默认遵守当时的信息可见性规则。

### 12.5 比赛结果

- 最终排名。
- 总手数和比赛时长。
- 真人玩家基础统计。
- 重新开始和返回首页。

第一阶段优先保证清晰、响应迅速和移动端可操作，不以复杂 3D 牌桌或大量动画作为完成条件。

## 13. 本地数据和版本管理

IndexedDB 至少保存：

- 应用设置。
- 锦标赛配置。
- 当前锦标赛快照。
- 事件日志。
- 已完成牌局摘要。
- 基础玩家统计。

存档必须包含数据版本号。读取旧存档时通过迁移器升级，无法升级时应提供明确提示，不能静默产生错误状态。

建议在每个已接受的玩家动作和每手牌结束后建立恢复点。写入失败不能破坏内存中的正在进行牌局。

由于第一阶段没有账号系统：

- 所有数据只保存在当前浏览器。
- 清理浏览器数据会删除存档。
- 不提供跨设备同步。
- 可在后续增加手动导出和导入，但不作为第一阶段强制要求。

## 14. 开发里程碑

### M0：项目初始化与算法验证

交付内容：

- React、TypeScript、Vite 项目。
- Vitest、fast-check 和 Playwright 配置。
- PWA 与 GitHub Pages 基础部署流程。
- 牌型计算候选实现的正确性、性能、许可证和浏览器兼容性验证。
- 模块边界和代码质量检查配置。

退出条件：项目可以构建、测试并发布一个空壳页面；牌型计算方案正式确定。

### M1：牌与牌型工具

交付内容：

- Card、Deck、HandRank 类型。
- 安全洗牌和可注入随机源。
- HandEvaluator 适配器。
- 底池赔率基础工具。
- 固定牌组与参考牌型测试。

退出条件：牌型、比较、平局和洗牌测试通过，第三方实现被完整封装。

### M2：单手牌规则引擎

交付内容：

- HandState、ActionIntent、GameEvent 和 Reducer。
- 盲注、发牌和四轮下注。
- 合法动作和最小加注。
- Fold、Check、Call、Bet、Raise、All-in。
- Heads-up 顺序。
- 主池、边池和摊牌结算。
- 玩家安全视图。

退出条件：可以无 UI 地运行大量完整手牌；规则场景与属性测试通过。

### M3：单桌锦标赛控制器

交付内容：

- TournamentState 和 TableState。
- 多手牌连续运行。
- 按手数升级盲注。
- 按钮移动、玩家淘汰和最终排名。
- 暂停与恢复所需的状态接口。

退出条件：随机动作代理可以批量跑完 2–9 人锦标赛，始终决出冠军且筹码守恒。

### M4：算法机器人与概率工具

交付内容：

- Player 统一接口。
- 简单、普通、困难机器人。
- 风格参数系统。
- Web Worker 胜率估算。
- 机器人非法动作回退。

退出条件：不同机器人组合可以稳定完成锦标赛，难度与风格差异能够通过行为统计观察到。

### M5：可玩牌桌和比赛配置

交付内容：

- 首页和新建锦标赛。
- 完整牌桌界面和真人操作控件。
- 机器人行动调度与基础过渡效果。
- 比赛状态、盲注和结果界面。
- 桌面与移动端适配。

退出条件：真人可以从创建比赛开始，完成一场完整锦标赛。

### M6：存档、记录、回放和统计

交付内容：

- IndexedDB Repository。
- 自动保存和恢复。
- 牌局列表、详情和基础回放。
- 真人基础统计和比赛结果摘要。
- 存档版本与迁移框架。

退出条件：刷新或关闭页面后可以恢复；已完成手牌可以按事件重建并回放。

### M7：质量加固与发布

交付内容：

- 完整规则回归测试。
- 长时间随机锦标赛模拟。
- 关键用户流程端到端测试。
- 性能、移动端、无障碍和离线检查。
- GitHub Pages 正式部署。
- 第一阶段使用说明和已知限制。

退出条件：满足本方案的验收标准，并发布可公开访问的静态版本。

## 15. 测试策略

### 15.1 单元测试

覆盖：

- 牌和牌型。
- 下注金额计算。
- 合法动作。
- 底池赔率。
- 边池拆分。
- 平分和奇数筹码。
- 盲注与按钮移动。
- 玩家视图过滤。
- 盲注级别与排名。

### 15.2 场景测试

用固定牌堆、固定座位和固定动作序列覆盖：

- 两人桌和多人桌。
- 翻牌前所有人弃牌。
- 单人和多人全下。
- 多个不同额度边池。
- 不足额全下不重新开放加注。
- 累积短码全下对行动权的影响。
- 平局与多个赢家。
- 同一手淘汰多个玩家。
- 从多人桌进入 heads-up。
- 最后一手决定冠军。

### 15.3 属性测试

通过随机生成合法动作序列持续验证：

- 筹码守恒。
- 牌的唯一性。
- 底池金额非负。
- 玩家投入不超过其可用筹码。
- 非当前玩家不能行动。
- 已弃牌或全下玩家不会再次行动。
- 一手牌最终一定可以进入终止状态。
- 重放结果与实时结果一致。

### 15.4 批量模拟

使用无 UI 机器人运行大量完整锦标赛，检查：

- 不出现死循环。
- 不出现无人可行动但牌局无法推进。
- 每场比赛最终恰好有一名冠军。
- 锦标赛开始与结束时筹码总数一致。
- 不同座位和人数均可稳定运行。

### 15.5 端到端测试

覆盖：

- 创建并开始比赛。
- 真人完成一次合法行动。
- 下注控件只允许合法数值。
- 暂停、刷新和恢复。
- 真人淘汰与比赛结束。
- 查看并回放牌局。
- GitHub Pages 子路径部署后资源正常加载。

## 16. 核心质量不变量

任何版本都必须满足：

```text
所有玩家筹码 + 当前底池 + 待返还筹码 = 锦标赛初始筹码总数

同一张牌在同一时刻只能存在于一个位置

非法命令不会改变状态或写入成功事件

玩家和机器人只能看到其权限允许的信息

相同初始状态 + 相同事件日志 = 相同最终状态

任意合法牌局最终都能结算或明确终止
```

## 17. 主要风险与控制方式

| 风险 | 控制方式 |
| --- | --- |
| 全下、最小加注和边池规则错误 | 固定场景测试、属性测试、参考实现交叉验证 |
| 引擎与 UI 相互污染 | 独立 Domain 模块，禁止依赖 React 和存储 |
| 机器人读取隐藏信息 | 强制只传入 PlayerView |
| 随机问题无法复现 | 测试使用种子随机源，正式发牌写入事件日志 |
| 存档升级后不可读取 | 数据版本号和迁移测试 |
| Monte Carlo 阻塞界面 | Web Worker、预算限制和取消机制 |
| 第一阶段范围膨胀 | 严格遵守“不包含范围”，现金桌和大模型延后 |
| GitHub Pages 路径或离线缓存问题 | 在部署环境执行端到端冒烟测试 |

## 18. 实施顺序结论

第一阶段按照以下主链路推进：

```text
工程基础
  ↓
牌与成熟牌型算法
  ↓
单手牌规则引擎
  ↓
单桌锦标赛生命周期
  ↓
算法机器人与概率工具
  ↓
可玩牌桌
  ↓
存档、记录和回放
  ↓
质量加固与 GitHub Pages 发布
```

规则引擎和自动化测试优先于完整 UI。M2 和 M3 达到退出条件后，再进入主要牌桌界面开发，避免界面建立在不稳定的规则模型上。

## 19. 实现状态（2026-09-17）

第一阶段本地实现已经完成，M0–M7 的功能与质量门禁均已落地：

- 自研 TypeScript 锦标赛/牌局引擎，并封装成熟的 `pokersolver` 牌型计算。
- 2–9 人、完整下注轮、短码全下、主池/边池、未跟注筹码退回、平分与奇数筹码、Heads-up、盲注升级、淘汰与排名。
- 三档难度和四种风格的算法机器人，机器人只接收玩家安全视图。
- React 响应式牌桌、真人座位配置、快捷下注、暂停/继续、概率和底池赔率面板。
- Web Worker Monte Carlo 估算、事件日志/检查点 Reducer、IndexedDB v3 自动恢复与已完成比赛归档。
- 基础统计、逐步回放、本地配置记忆、PWA 离线壳和 GitHub Pages 工作流。
- 19 个 Vitest 单元/场景/属性/批量模拟测试，以及 10 个桌面/移动 Playwright 生产构建流程测试全部通过。
- 生产依赖审计为 0 个已知漏洞。

当前目录尚未关联 Git 仓库或 GitHub 远端，因此没有生成公开 URL；推送到 GitHub `main` 分支并启用 Pages 后，现有工作流即可执行验证与部署。
