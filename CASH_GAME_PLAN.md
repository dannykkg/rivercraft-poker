# Rivercraft 现金桌接入方案

## 1. 目标与首版范围

在现有单机单桌德州扑克中增加与“单桌锦标赛”并列的“现金桌”模式，同时保持扑克规则内核唯一，避免复制下注、边池、摊牌和发牌逻辑。

首版现金桌范围：

- 2～9 人单桌，人类玩家 1 名，其余为本地机器人。
- 固定盲注，可选择 `5/10`、`10/20`、`25/50`、`50/100`。
- 买入深度可选择 `40BB`、`100BB`、`200BB`。
- 用户输光后可以按初始买入重新买入，或结束本次现金桌。
- 机器人输光后在下一手开始前自动按初始买入补充筹码。
- 用户可在任意一手结束后主动结束牌局并查看会话结算。
- 记录总买入、当前筹码、净盈亏、手数、胜负底池数和 BB/100。
- 复用现有发牌动画、30 秒行动钟、语音、机器人、胜率、操作建议、记录与离线存档。

首版不包含真实货币、跨设备同步、账号、匹配大厅、真人网络通信和中途换座。这些能力通过边界设计预留，不在本次实现中制造伪后端。

## 2. 领域边界

### 2.1 共享扑克内核

以下规则对两种模式完全共享：

- 52 张牌、洗牌、发牌和烧牌。
- 盲注提交、行动顺序、合法动作校验。
- 弃牌、过牌、跟注、加注、全下。
- 轮次推进、主池和边池、未跟注筹码返还。
- 摊牌、牌型比较和底池分配。

共享内核不得读取账号、网络连接或界面状态。

### 2.2 模式规则

配置使用可辨识联合类型：

```ts
type GameConfig = TournamentConfig | CashGameConfig;
```

锦标赛负责：盲注升级、淘汰、名次、冠军和终局。

现金桌负责：固定盲注、买入账本、自动补充、用户重新买入、主动离桌和会话盈亏。

模式差异集中在以下命令和策略中：

- 获取当前盲注。
- 本手结束后的玩家处理。
- 下一手开始前的座位准备。
- 整场游戏的结束条件。

不允许在牌型计算或下注代码中加入现金桌专用分支。

### 2.3 应用层

React 应用只负责：

- 将用户操作转换为领域命令。
- 播放领域状态对应的动画和声音。
- 展示设置、会话统计、重新买入及结算。
- 调用本地存储适配器。

机器人属于当前应用层的行动来源，和未来真人网络玩家一样，只提交标准 `PlayerAction`，不能直接修改状态。

## 3. 数据模型

### 3.1 配置

```ts
interface BaseGameConfig {
  mode: "tournament" | "cash";
  players: PlayerConfig[];
  startingStack: number;
}

interface TournamentConfig extends BaseGameConfig {
  mode: "tournament";
  blindLevels: BlindLevel[];
}

interface CashGameConfig extends BaseGameConfig {
  mode: "cash";
  smallBlind: number;
  bigBlind: number;
  buyIn: number;
  minBuyIn: number;
  maxBuyIn: number;
  botAutoRebuy: boolean;
}
```

### 3.2 现金桌会话账本

```ts
interface CashGameSession {
  startedAt: number;
  endedAt?: number;
  ledgers: Record<string, {
    totalBuyIn: number;
    rebuyCount: number;
    potsWon: number;
  }>;
}
```

净盈亏始终由 `当前筹码 - 总买入` 推导，避免保存重复且可能不一致的数据。

### 3.3 兼容命名

本次保留现有 `TournamentState` 类型名作为兼容别名，新增通用 `GameState` 概念。后续服务端化时外部协议使用 `GameState`，避免网络接口继承历史命名。

## 4. 现金桌生命周期

1. 创建现金桌，所有玩家买入同一初始额度。
2. 使用固定盲注开始第一手。
3. 每手按共享扑克规则运行和结算。
4. 本手结束：
   - 不产生淘汰和名次。
   - 记录赢得底池的玩家。
   - 机器人筹码为零时，下一手前自动重新买入。
   - 用户筹码为零时，阻止开始下一手并显示重新买入/结束选项。
5. 用户结束牌局时生成会话结算，状态进入 `finished`，保存至历史记录。

重新买入只能发生在两手之间，避免改变一手中的有效筹码和边池资格。

## 5. 界面方案

- 新建牌局增加“锦标赛 / 现金桌”切换。
- 锦标赛继续显示初始筹码与盲注速度。
- 现金桌显示固定盲注、买入深度和实际买入金额。
- 游戏顶部根据模式显示“第 N 手牌”以及盲注信息。
- 现金桌侧栏用“会话盈亏”代替“排名与筹码”，展示用户和机器人的总买入、当前筹码、净盈亏。
- 用户筹码归零后使用现金桌专用弹窗：重新买入或结束牌局。
- 现金桌提供“结束牌局”按钮，仅在一手结束后执行。
- 最终弹窗展示会话手数、总买入、带走筹码、净盈亏和 BB/100。

## 6. 存档与迁移

- 新事件和检查点继续使用事件日志存储。
- 新存档版本为 `4`。
- 旧版本配置缺少 `mode` 时一律迁移为 `tournament`。
- 历史列表同时支持锦标赛和现金桌，标签明确区分模式。
- 本地存储只是适配器；领域引擎不直接访问 IndexedDB。

## 7. 为账号和真人联网预留的边界

### 7.1 身份

领域层只识别稳定的 `playerId`，不保存用户名、令牌或账号资料。未来账号系统通过身份映射把 `userId` 绑定到牌桌 `playerId`。

### 7.2 服务端权威

当前由本地应用调用领域命令；未来可将同一命令移到服务端执行：

```text
客户端操作 → Command Transport → 权威游戏服务 → GameEvent[] → 客户端投影
```

界面不依赖机器人或真人的具体来源，只依赖公开玩家状态和合法动作。

### 7.3 隐藏信息

底牌继续通过玩家视图投影控制。未来网络模式中，服务端只向对应连接发送私有底牌，公共事件不包含其他玩家底牌。

### 7.4 持久化替换

当前 `repository` 是 IndexedDB 实现。未来增加账号后，可以新增 HTTP/WebSocket repository，而无需修改扑克内核和主要界面状态结构。

本次代码已经落地 `GameRepository` 端口，当前注入 `indexedDbGameRepository` 浏览器适配器；服务端化时可保持调用方不变并替换适配器。

## 8. 验收标准

- 两种模式可以独立创建、保存、恢复和结束。
- 旧锦标赛存档仍能恢复。
- 现金桌盲注不会升级，不产生淘汰或冠军。
- 机器人输光后能在下一手前自动买入。
- 用户输光后不能绕过重新买入直接开始下一手。
- 重新买入正确增加总买入和筹码。
- 结束现金桌后盈亏与 `筹码 - 总买入` 一致。
- 发牌、行动、边池、胜率、建议、倒计时和自动下一手在两种模式中均可用。
- 单元测试覆盖现金桌生命周期和旧存档迁移，端到端测试覆盖创建、重新买入、结束与恢复。
