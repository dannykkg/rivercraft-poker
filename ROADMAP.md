# AI 单人德州扑克：产品方向与三阶段路线

## 1. 项目目标

本项目计划实现一款 AI 驱动的单人德州扑克游戏。玩家可以自定义牌桌人数，并为不同座位选择不同类型、难度和风格的机器人。后续可配置多个 AI 供应商和模型，并将指定的供应商与模型分配给不同 AI 玩家。

项目总体演进思路是：

> 第一阶段建立可靠的扑克世界；第二阶段让 AI 在这个世界中使用工具决策；第三阶段让 AI 从长期记录中理解玩家并担任教练。

游戏规则、数学计算和状态推进由确定性的程序负责。AI 只能在合法动作范围内进行决策，不能直接控制发牌、结算或修改游戏状态。

## 2. 第一阶段：经典德州扑克

第一阶段的目标是完成一个不依赖大模型、可以独立游玩的经典单人德州扑克游戏。

第一阶段已确定采用传统无限注德州扑克单桌锦标赛模式：每桌 2–9 人，其中一名真人玩家，其余为算法机器人；不包含账号登录系统。现金桌作为未来可新增的独立模式，暂不进入第一阶段范围。详细实施方案见 [`PHASE1_PLAN.md`](./PHASE1_PLAN.md)。

主要能力包括：

- 完整且正确的德州扑克规则和牌局流程。
- 支持自定义真人与机器人玩家数量。
- 提供多个难度等级的算法机器人。
- 提供保守、松散、激进、诈唬等不同机器人风格。
- 提供手牌胜率、底池赔率等基础计算能力。
- 保存完整的牌局记录、玩家行动和结算结果。
- 支持牌局回放、基础统计、游戏保存与恢复。

这一阶段不以打造顶级扑克 AI 为目标，而是优先保证规则正确、机器人行为合理、难度可控、游戏运行稳定。

### 游戏记录的定位

游戏记录不是附加功能，而是项目底层能力的一部分。每次发牌、下注、跟注、加注、弃牌和结算都应记录为结构化事件。

统一的事件记录将为以下能力提供基础：

- 牌局保存和恢复。
- 完整牌局回放。
- 玩家行为统计。
- AI 玩家画像。
- 决策质量分析。
- 不同策略的模拟和复盘。
- 游戏问题排查。

## 3. 第二阶段：可使用工具的 AI 机器人

第二阶段的目标是引入由外部 AI 模型驱动的机器人玩家。每个 AI 座位可以配置不同的供应商、模型和角色风格。

AI 不自行执行底层数学计算，而是根据牌局情况选择并调用系统提供的可靠工具，再综合工具结果作出决策。

可提供给 AI 的工具方向包括：

- 手牌强度计算。
- 胜率模拟与估算。
- 底池赔率计算。
- 合法动作和下注范围查询。
- 当前牌桌位置与筹码深度查询。
- 对手行动历史查询。
- 对手牌力范围估计。
- 已知玩家画像查询。

典型决策流程为：

```text
轮到 AI 行动
    ↓
AI 获取当前座位允许看到的信息
    ↓
AI 选择并调用计算或历史工具
    ↓
系统返回确定、结构化的结果
    ↓
AI 在合法动作中选择弃牌、过牌、跟注或加注
    ↓
扑克引擎校验并执行动作
```

### AI 玩家的边界

- AI 只能获取当前座位有权看到的信息。
- AI 不能读取其他玩家底牌或未发出的牌。
- AI 不能直接修改游戏状态。
- AI 只能从引擎提供的合法动作中选择。
- AI 输出必须经过扑克引擎校验。
- AI 超时、调用失败或输出无效时，由内置算法机器人接管。

第二阶段的目标不只是提升机器人强度，也包括形成不同性格、不同分析方式和具有一定可解释性的 AI 对手。

## 4. 第三阶段：玩家画像与 AI 教练

第三阶段的目标是让 AI 基于长期牌局记录分析真人玩家的行为模式，并在牌局结束后提供复盘和改进建议。

牌桌 AI 与教练 AI 应当是两个独立角色：

```text
AI 对手：只获取当前玩家视角，目标是参与并赢得牌局。
AI 教练：在牌局结束后读取完整记录，目标是分析和教学。
```

AI 教练可以使用以下类型的算法和工具：

- 起手牌范围分析。
- 不同位置的行动统计。
- 玩家长期行为指标。
- 胜率和底池赔率计算。
- 下注尺度分析。
- 对手范围推演。
- 相似历史牌局检索。
- 不同决策的模拟对比。
- 期望收益的近似评估。

AI 教练可以逐步识别玩家的倾向，例如过度跟注、特定位置开池不足、面对大额下注时弃牌率异常，或者对某类对手长期采用不合适的策略。

分析结果应明确区分：

- 数学上可以明确判断的问题。
- 依赖策略假设的建议。
- 基于有限样本推断的行为倾向。

底层统计和计算仍由确定性工具完成，AI 主要负责选择分析方向、综合结果，并以容易理解的方式解释建议。

## 5. 三阶段之间的依赖关系

```text
第一阶段
规则引擎 + 算法机器人 + 事件记录 + 基础计算工具
                         ↓
第二阶段
AI 调用已有工具，作为牌桌玩家进行决策
                         ↓
第三阶段
AI 使用完整记录与分析工具，建立玩家画像并提供建议
```

第一阶段不是临时版本。它建立的规则环境、数据系统和计算工具，会成为后续 AI 玩家和 AI 教练共同依赖的平台能力。

## 6. 需要尽早稳定的核心接口

虽然具体实现细节将在后续设计中确定，但应尽早形成三个稳定的抽象边界：

### Player

统一真人玩家、算法机器人和 AI 机器人的行动方式。所有玩家都通过相同接口接收自己的牌局视角并提交动作。

### PokerTool

统一胜率、赔率、合法动作、历史统计和策略分析等工具，使算法机器人、AI 玩家和 AI 教练可以复用相同能力。

### GameEvent

统一记录牌局中的每一个状态变化，为保存、恢复、回放、统计、玩家画像和复盘提供可信的数据来源。

## 7. 已确认的技术选型

项目第一阶段采用纯 Web 方案，优先保证浏览器运行、离线单机体验和 GitHub Pages 静态部署能力。

初步技术栈如下：

| 领域 | 选型 |
| --- | --- |
| 开发语言 | TypeScript |
| Web 界面 | React |
| 构建工具 | Vite |
| 应用形态 | Web / PWA |
| 状态管理 | React Reducer 或轻量状态库，具体实现时确定 |
| 本地持久化 | IndexedDB |
| 单元测试 | Vitest |
| 属性测试 | fast-check |
| 静态托管 | GitHub Pages |
| AI 接入 | 自研 Provider Adapter，支持多个供应商和模型 |
| 可选服务端 | 后期按需增加 API Proxy，不作为第一阶段依赖 |

选择纯 Web 而不是 Godot，主要是因为当前产品包含大量牌桌配置、模型管理、游戏记录、统计、回放和分析界面。Web 技术更适合此类交互，也更容易以静态应用形式发布。

### 核心引擎决策

扑克游戏引擎由项目自行开发，不直接绑定或改造某个完整的第三方扑克引擎。

自研范围包括：

- 游戏状态和阶段推进。
- 玩家行动顺序与合法动作验证。
- 下注轮次、最小加注和全下规则。
- 主池、边池、退款和平分底池。
- 庄家与盲注位置移动。
- 玩家视角和隐藏信息隔离。
- 结构化事件记录。
- 保存、恢复、回放和模拟模式。
- 真人玩家、算法机器人和 AI 机器人的统一接入接口。

成熟、独立且已有充分验证的底层算法可以复用，不要求重复实现。优先考虑复用或参考的能力包括：

- 五张牌和七张牌的牌型计算与比较。
- 基于 Web Crypto 的安全随机数和洗牌基础能力。
- Monte Carlo 胜率估算方法。
- 已有开源实现中的规则场景和测试用例。
- 参考扑克实现生成的交叉验证结果。

采用这一边界后，项目仍然拥有自己的扑克引擎：核心状态、规则流程、数据模型和对外接口均由项目控制；外部算法只作为可替换的纯计算组件使用。

牌型计算库等具体依赖将在实现前通过正确性、浏览器兼容性、性能、维护状态和许可证检查后确定。

## 8. 技术架构简介

整体架构采用分层设计，使扑克规则、机器人决策、AI 模型和用户界面彼此解耦。

```text
Web / PWA 界面
├── 牌桌与玩家操作
├── 新游戏与机器人配置
├── Provider / Model 配置
├── 牌局记录与回放
└── 玩家统计与教练界面
                ↓
应用编排层
├── 游戏会话
├── 玩家控制器
├── 存档与恢复
├── 回放与模拟
└── AI 请求调度与失败回退
                ↓
自研扑克引擎
├── 确定性状态机
├── 合法动作与下注规则
├── 底池与结算
├── 玩家安全视图
└── GameEvent 事件日志
          ↓                 ↓
扑克计算工具             玩家实现
├── 牌型计算             ├── 真人玩家
├── 胜率估算             ├── 算法机器人
├── 底池赔率             └── AI 机器人
├── 范围与统计
└── 策略分析
                ↓
基础设施
├── IndexedDB
├── AI Provider Adapters
├── Web Crypto
└── 可选 API Proxy
```

### 核心状态流

所有玩家，不论是真人、算法机器人还是 AI 机器人，都不能直接修改游戏状态。统一状态流为：

```text
玩家读取自己的 PlayerView
        ↓
玩家提交 ActionIntent
        ↓
引擎检查当前行动者、动作类型和下注额度
        ↓
合法动作被转换为一个或多个 GameEvent
        ↓
GameEvent 经过纯 Reducer 生成新的 GameState
        ↓
系统为不同玩家生成各自的 PlayerView
```

事件日志是牌局事实来源。洗牌和发牌结果也必须以可重放的形式记录；相同的初始状态和事件序列，应当始终产生相同的游戏状态，从而支持可靠的存档、回放、问题复现和策略模拟。

### 主要模块边界

#### Engine

只负责规则、状态和事件，不依赖 React、网络、外部 AI 或具体存储实现。引擎应能够在测试和模拟环境中独立运行。

#### PokerTool

封装牌型、胜率、赔率、范围和统计等可复用计算能力。算法机器人、AI 机器人和 AI 教练通过统一接口使用这些工具。

#### Player

统一不同玩家实现。玩家接收经过权限过滤的视图，并返回行动意图；最终合法性始终由引擎判断。

#### AI Provider

统一不同模型供应商的调用方式，负责模型配置、结构化输出、超时、重试和错误转换。供应商逻辑不能进入扑克引擎。

#### History and Analysis

基于 GameEvent 生成回放、长期统计、玩家画像和局后分析。它可以读取已结束牌局的完整信息，但牌桌 AI 在实时行动时只能读取自己的玩家视角。

### 引擎质量要求

自研引擎必须通过场景测试和属性测试持续验证以下不变量：

- 玩家筹码与所有底池筹码之和保持守恒。
- 同一张牌不能同时出现在两个位置。
- 只有当前行动玩家能够提交有效动作。
- 非法动作不能改变游戏状态。
- 相同输入事件必须产生相同结果。
- 玩家视图不能泄露无权查看的底牌或牌堆。
- 从事件日志重建的状态必须与实时状态一致。

## 9. 总体原则

- 先保证扑克规则和结算正确，再提高 AI 能力。
- 先建立可靠的算法机器人，再接入外部 AI 模型。
- 数学与规则交给确定性程序，判断与解释交给 AI。
- AI 对手严格遵守玩家视角，不能获得隐藏信息。
- AI 教练与牌桌 AI 分离，避免权限和目标混淆。
- 所有 AI 动作都必须经过扑克引擎验证。
- 每个阶段都应形成一个可以独立体验和验证的版本。

## 10. 当前方向结论

项目采用三阶段演进方案：

1. 实现经典德州扑克、不同级别和风格的算法机器人、基础概率计算及完整牌局记录。
2. 接入可以自主选择和调用扑克工具的 AI 机器人，并支持为不同座位配置不同供应商和模型。
3. 基于长期游戏记录构建玩家画像，由 AI 使用统计和策略工具提供复盘与训练建议。

技术上采用 TypeScript、React 和 Vite 构建可部署至 GitHub Pages 的 Web/PWA。扑克引擎采用项目自研的确定性事件驱动状态机，核心规则和数据接口由项目掌握；牌型计算、安全随机数、概率估算方法等成熟底层算法允许采用经过验证的开源实现或标准能力。

具体依赖库、游戏规则范围、机器人难度模型、AI 工具协议和界面设计，将在后续阶段分别形成更详细的设计文档。
