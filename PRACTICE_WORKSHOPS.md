# 范围构建与混合频率专项

在现有独立牌桌训练上追加两种专项，不改动基础课程、牌桌训练运行器或现有牌谱事实。入口：学习中心 → 实战训练 → 范围与频率，直接地址 `#learn/practice/skills`。

## 169格范围重建

两个任务分别对应基础课程中已明确标记的前位／按钮位保守核心范围。直接读取 `PREFLOP_BASELINES`，不更改原文、题目ID和基础课进度。

用户在完整13×13矩阵选择手牌类别，提交后显示漏选、多选及真实组合数：对子6、同花4、非同花12，总计1326个两张牌组合。空选合法，但会反馈缺失；没有通过“全选全部手牌”获得高正确率的指标。

**目标是重建指定教学范围，不是训练完整开池表。** 保守核心范围是项目自行编排的教学内容，不是授权GTO数据或独立专业审校成果。差异不称为通用策略错误，也不制造EV损失。

## 有限模型的混合频率

4组参数 × 2个角色，共8个专项。先保存0–100%的计划比例，再连续作出24次过牌／下注或弃牌／跟注选择。第一步执行后计划锁定。结束才显示参考、计划和执行比例；引导参考可以提前查看，但会记录辅助状态。

模型明确限定为：两人河牌、只有一个下注尺度，价值牌总胜抓诈牌、弱牌总输；无平局、阻断牌、加注、抽水和后续下注。不是完整扑克牌谱或职业策略求解器。V/L表示价值牌／弱牌的相对质量，不导入商业课程或策略文件。

令P为下注前底池，B为下注额，V/L为两类牌的质量。价值牌全部下注，弱牌下注比例为x，防守方跟注频率为q。内部混合解满足：

- `q=P/(P+B)`，令弱牌 `EV(bet)=(1-q)P-qB=0=EV(check)`。
- `x=VB/[L(P+B)]`，令下注范围诈唬份额 `alpha=Lx/(V+Lx)=B/(P+2B)`。
- 防守方 `EV(call)=alpha(P+2B)-B=0=EV(fold)`。

只接受 `0<x<1` 的内部解，不把边界解强行截断成混合练习。弱牌下注比例x不是下注范围中的诈唬份额alpha；两者在界面中分别说明。测试包含独立的有限牌组合CFR迭代交叉验证，不是将存储的答案再次交给同一个判题器。

24次选择只描述本组样本。计划偏差与执行偏差分别报告，不根据小样本给出统计显著错误，不将频率偏差称为EV损失，不把最高频动作设为唯一正确。这里不提供完整手牌范围的混合策略认证。

## 来源

- 范围：原 `study/curriculum.ts` 的显式教学核心范围，未修改。
- 数学：上述模型的无差异条件推导，由 `workshops/model.ts` 计算、单元测试核验。
- [GTO Wizard: How to use the Trainer](https://help.gtowizard.com/how-to-use-the-trainer/)：仅参考动作输入与频率输入的交互区分，未复制其策略数据。

## 数据隔离和恢复

新增独立数据库 `rivercraft-practice-workshops`，包含当前专项、完成记录和已见标记。矩阵每次选择和频率每次执行在事务完成后更新界面；频率计划需显式点击保存。修改中的未保存输入在界面中注明。

已见标记在开始时保存，即使放弃再开也不变成首次。查看参考会永久标记当前次辅助；完成后的答案展示不回溯改写成绩。旧标签页使用过期revision写入会失败，而不是覆盖较新状态。已完成记录不可修改。

专项备份格式为 `rivercraft-practice-workshops`，与牌桌实战备份、基础学习牌谱备份分开。导入检查版本、任务ID、动作、频率和记录一致性，按ID合并新增，不覆盖本地当前进度。跨数据库没有虚构的全局事务：完整迁移须分别导出这三个明确命名的进度包。

正在进行的牌桌测验不能切换到基础知识或范围频率参考，须完成或主动结束；引导牌局切换课程会标为使用辅助。课程文件与成绩本身不修改。

## 验证

单元测试检查169类/1326组合、范围展开、部分组合拒绝、内部混合解、双边无差异、独立CFR比较、版本和备份校验。桌面与手机浏览器检查实际点击、24次执行、刷新恢复、辅助/重复记录、旧标签页写入冲突、备份幂等和未访问课程的离线加载。

所有最终验证以PR对应提交的GitHub Actions为准；测试通过不等于未经独立审校的教学范围已经变成专业策略包。
